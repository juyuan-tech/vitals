---
name: 功能请求
about: 想要一个模块、一个选项、一种输出
title: "[feature] "
labels: enhancement
---

**想要什么**

**为什么现在的做法不够用**

**数据从哪来**

请说明这个功能**读什么文件、或调用什么系统调用**。这个项目不执行外部命令、不发网络包、
不新增依赖，所以「调用 `xxx` 命令取数」这类方案需要先说清为什么不能用文件或系统调用替代。

**（如果是新模块）它离开这台机器会怎样**

即：在容器里、在 CI 的 headless 环境里、在没有对应硬件的机器上，它应该「空」还是「失败」？

**需要动到项目底线吗**

- [ ] 需要新增依赖
- [ ] 需要 `unsafe`
- [ ] 需要执行外部命令
- [ ] 需要发网络请求

（勾了任何一项，请在上面的说明里论证必要性；这四项默认都不接受。）
